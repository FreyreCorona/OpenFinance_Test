name: consumer-build

on:
  push:
    branches: [master]
    paths:
      - 'src/cmd/consumer/**'
      - 'src/internal/**'
      - 'src/go.*'
      - 'src/Dockerfile.consumer'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          docker build -t ghcr.io/${{ lower(github.repository) }}/consumer:latest -f src/Dockerfile.consumer src/
          docker push ghcr.io/${{ lower(github.repository) }}/consumer:latest